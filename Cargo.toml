[package]
name = "sentinel-bridge"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.0", features = ["full"] }
tokio-stream = "0.1"
redis = { version = "0.25", features = ["tokio-comp"] }
