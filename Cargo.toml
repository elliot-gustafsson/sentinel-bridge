[package]
name = "sentinel-bridge"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.0", features = ["full"] }
tokio-stream = "0.1"
fred = { version = "9.0", features = ["subscriber-client"] }
